# shibuya — состояние проекта

> Файл для передачи контекста в новый чат. Актуально на **2026-08-06**.
> Рабочие инструкции — в `RUNBOOK.md`, он рядом.

## Что это

Raspberry Pi 5 (Ubuntu 24.04 arm64, hostname `shibuya`) как постоянно доступный
рабочий сервер: заходить с iPhone через Blink Shell по mosh и полноценно работать,
когда нет ноутбука — тушить инциденты в проде, разбирать PR, запускать Claude Code.

Машина стоит дома, но готовится к переезду к родителям, где будет подключена к
ИБП и проброшена с их белого статического IP.

**Ключевая идея:** два независимых канала доступа (Tailscale + проброс портов),
mosh для переживания смены сети, tmux для переживания всего остального.

## Статус: Фазы A и B закрыты

`~/shibuya/verify.sh` → **64 пройдено / 0 замечаний / 0 провалов**

Проверено не «по факту установки», а реальной работой:

| Что | Как проверено |
|---|---|
| mosh с телефона | Blink → mosh → tmux; **переключение WiFi→LTE посреди сессии сессию не рвёт** |
| mosh через Tailscale | подключение по MagicDNS-имени `shibuya`, файл-доказательство записан |
| git | клон `liven-dev/gh-slack-bot` через `github.com-lvn`, identity подставилась сама |
| kubectl | 3 GKE-кластера отвечают, включая оба прода |
| claude / k9s / docker | `claude -p` отвечает, k9s стартует, `docker run hello-world` проходит |
| перезагрузка | ~20 секунд простоя, всё поднялось само |
| самолечение сети | прогнано в песочнице: рестарт сети на 3-м провале, ребут на 6-м, cooldown держит |
| Telegram | уведомление при загрузке, heartbeat 09:00, netcheck каждые 5 минут |
| netplan | оба SSID (`autostrada`, `vb_kiev`) в wpa_supplicant, eth0 metric 100 / wlan0 600 |

## Как подключиться

| Откуда | Команда |
|---|---|
| Мак, дома | `mosh -p 60000:60010 shibuya-local` |
| Мак, Tailscale | `mosh -p 60000:60010 shibuya` |
| iPhone | Blink → профиль `shibuya` (mosh, порты `60000:60010`) |

Вход сразу попадает в tmux-сессию `main`. **Выход — `Ctrl-a` затем `d`.**
Если набрать `exit` в последнем окне, он тоже отцепит, а не убьёт сессию
(перехват в `/etc/shibuya/shell-tmux.sh`). Настоящий выход — `builtin exit`.

## Как вносить изменения

Систему руками не трогаем — правим скрипт и раскатываем. Иначе теряется
воспроизводимость, а на ней держится будущая миграция на SSD.

```bash
cd ~/shibuya
./deploy.sh --run              # синхронизировать и прогнать фазу A
./deploy.sh --run --only 20    # один скрипт
./bootstrap.sh --list          # что вообще есть
ssh shibuya '~/shibuya/verify.sh'
```

Всё идемпотентно, повторный прогон безопасен. Оригиналы тронутых системных
файлов — в `/etc/shibuya/backups/`. Репозиторий: `~/shibuya` (git, локальный,
намеренно вне `~/lvn`, чтобы не попасть под git-контекст Skelar).

## Что осталось

### Перед отвозом

- [x] Закрыть сессию на tty1
- [ ] **Образ SD-карты** — точка отката. `sudo poweroff`, карту в кардридер:
      `sudo dd if=/dev/rdiskN bs=4m | gzip > ~/shibuya-$(date +%F).img.gz`
- [ ] **ИБП**: если есть USB-порт **данных** — подключить и `sudo ~/shibuya/bootstrap.sh --only 90`
      (корректное выключение при разряде + алерт о пропаже света).
      Если порта нет — пропустить, но жёсткое выключение бьёт ФС на SD.
- [ ] Распечатать памятку родителям из `RUNBOOK.md`, вписать телефон

### На месте у родителей (Фаза C)

1. Питание через ИБП, **Ethernet-кабель** в роутер
2. Проверить доступ через Tailscale (проброса ещё нет — тут он и окупается)
3. На роутере: **DHCP-резервация** по MAC eth0 `2c:cf:67:1c:3e:6a`
   (wlan0 `2c:cf:67:1c:3e:6b`)
4. Проброс:
   - **TCP** `<высокий внешний порт>` → `22`
   - **UDP** `60000-60010` → `60000-60010` — **строго один-в-один**: mosh-server
     сообщает клиенту номер занятого порта, подмена номеров всё ломает
5. **Приёмка: выключить Tailscale на телефоне** и подключиться по LTE на белый IP.
   Иначе легко решить, что проброс работает, когда трафик идёт по тайлнету.
6. Раскомментировать `Host shibuya-wan` в `~/.ssh/config` на Маке, завести
   такой же профиль в Blink

### Позже — переезд на SSD (Фаза D)

Порядок ради которого всё делалось скриптами: прошить чистую Ubuntu 24.04 на SSD →
загрузиться → `./deploy.sh --run` → перелогиниться в gcloud/gh/claude/tailscale →
перенести `~/lvn` и `/etc/shibuya`. Надёжнее клонирования живой карты и заодно
проверяет, что скрипты действительно воспроизводят систему.

После: `data-root` докера на SSD, поднять лимиты journald, при необходимости
`rpi-eeprom-config` → `BOOT_ORDER`.

## Какой SSD купить

Сейчас система на SD-карте (`mmcblk0`, 29 ГБ, занято 7.3 ГБ = 27%). Карта —
расходник: деградирует от записи и через год-два уходит в read-only. Смягчено
(логи ограничены 200 МБ, swap в zram, `noatime`), но не решено.

**Объём: 512 ГБ.** Проверено по ценам (август 2026): 256 ГБ на Розетке стоит
столько же, сколько 512 — дешёвые Kioxia BG4/BG6 на 256 числятся «нет в наличии»,
а реально доступные идут по 3 850–4 823 ₴ против 4 085 ₴ за 512. Экономить на
объёме нечем.

**Обязательно TLC, не QLC.** TLC — 3 бита в ячейке, ресурс ~1000–3000 циклов записи;
QLC — 4 бита и ~300–1000 циклов. При круглосуточной записи логов это разница между
«работает пять лет» и «через полтора года начались странности».

### Список покупки — две позиции, ~4 800 ₴

**Важно про имеющееся железо:** на Pi стоит официальный корпус Pi 5, у которого
**вентилятор встроен в крышку** (не Active Cooler — радиатора на процессоре нет).
Система это подтверждает: `cooling_fan` крутится 3059 об/мин, температура 56.8°C.
Поэтому вариант с M.2 HAT+ отпадает: HAT занимает место крышки, а снять крышку =
остаться без охлаждения вообще.

| Позиция | Модель | Цена |
|---|---|---|
| Корпус + охлаждение + NVMe | **Argon ONE V3 M.2 NVMe** | ~1 900 ₴ (OLX, продавец Андрій — 5.0/5, 100+ доставок) |
| Диск | **SanDisk IX SN530 256 ГБ** (`SDBPNPZ-256G-XI`) | ~2 900 ₴ |

**Почему Argon ONE V3, а не NEO 5 (1 500 ₴).** Доплата 400 ₴ покупает **кнопку
питания с корректным выключением**: родителям можно сказать «нажми и держи кнопку
три секунды», вместо «выдерни шнур». Жёсткий обрыв питания — главный способ побить
ФС, и мы отдельно закладывались на этот риск. Также: радиатор на сам NVMe,
PWM-вентилятор, порты выведены назад, диск на шлейфе **внутри** корпуса (внешнего
кабеля, который можно задеть, нет). Кнопка работает через открытый скрипт Argon —
его установку нужно добавить в провижининг отдельным шагом.

**Почему этот диск.** В артикуле `SDBPNPZ` буква `N` = формат **2280** (вариант 2230
идёт как `SDBPTPZ`), значит встаёт в Argon штатно, без перестановки стойки. «IX» —
**Industrial-линейка**: рабочий диапазон −40…+85 °C, стойкость к вибрации, серия
рассчитана на круглосуточную работу во встраиваемых системах. Это прямо релевантно —
коробка будет годами стоять в непонятно как проветриваемом углу. 3D TLC,
**400 TBW на 256 ГБ** — вдвое больше, чем у бытовых дисков на 500 ГБ.

256 ГБ достаточно: сейчас занято 7.3 ГБ, тридцатикратный запас. PCIe Gen 3 и запись
900 МБ/с не смущают — у Pi 5 линия x1, упирается в ~450–900 МБ/с при любом диске.

**Уточнить у продавца: гарантию.** Industrial-линейка продаётся как OEM-компонент,
розничная гарантия бывает не оформлена.

**Докупать больше нечего:** блок питания 27 Вт (5.1V/5A) есть, SD-карта остаётся
в слоте как аварийный носитель. Нынешний корпус с крышкой-вентилятором — в запас.

#### Как выбирать диск, если этот не купится

Рамка: **256–500 ГБ, M.2 2280, NVMe, обязательно TLC.** Тип памяти смотреть в
карточке товара, а не выводить из цены — Goodram PX600 500 ГБ стоит 3 966 ₴ и при
этом **QLC**, а Kioxia Exceria G2 500 ГБ стоил 1 832 ₴ и TLC (в наличии не было).
**Kingston NV2/NV3 обходить** — меняют начинку без смены артикула. Ноунеймы
(Huadisk и подобные) — тип памяти не проверить, гарантийного пути нет.

Про TBW без паники: журнал ограничен 200 МБ, у докера лимиты — машина пишет
~1–5 ГБ в сутки, то есть ~2 ТБ в год. Даже 160 TBW это десятилетия. Ресурс здесь
не связывающее ограничение, важнее предсказуемость бренда и гарантия.

Если попадётся OEM-выпайка из ноутбуков (WD SN740 и подобные) — брать только у
продавца с возвратом и сразу проверить пробег:
```bash
sudo apt install -y nvme-cli
sudo nvme smart-log /dev/nvme0n1 | grep -E 'percentage_used|power_on_hours'
```
У нового `percentage_used` = 0%, `power_on_hours` — единицы. Сотни часов = выпайка.

Отвергнутые варианты: **M.2 HAT+ + Active Cooler** (~5 550 ₴, плата без корпуса,
несовместимо с имеющейся крышкой-вентилятором); **USB-бокс + SATA SSD**
(~3 000–5 300 ₴, дешевле, но внешний кабель как точка отказа на машине, к которой
не подъехать); **комплект Argon NEO 5 + Huadisk 512 ГБ за 6 670 ₴** (диск выходит
в ~5 170 ₴, ноунейм, продавец без рейтинга, «Вживане» при заголовке «НОВЫЙ»).

### Не переплачивать за скорость

У Pi 5 линия PCIe шириной x1: Gen2 даёт ~450 МБ/с, Gen3 (официально не поддерживается,
обычно работает) ~900 МБ/с. Упирается в шину, а не в диск — Gen4-накопитель не даст
ничего сверх Gen3. Даже нижние ~450 МБ/с против SD-карты это примерно двадцатикратная
разница.

Если диск не определяется — оставить Gen2 по умолчанию, не выставлять
`dtparam=pciex1_gen=3` в `/boot/firmware/config.txt`.

### Питание — уже есть

Официальный **Raspberry Pi 27 Вт USB-C (5.1V ⎓ 5.0A, P1724)** — в наличии, подходит.
Покупать ничего не нужно. Обычная зарядка 5V/3A дала бы случайные перезагрузки под
нагрузкой — худший класс проблем для недоступной машины.

### После установки диска

```bash
sudo rpi-eeprom-config --edit    # BOOT_ORDER=0xf416 — сначала NVMe, потом SD
```
**SD-карту оставить в слоте** как аварийный носитель: при проблемах с NVMe машина
сама свалится на загрузку с неё.

## Что было найдено по дороге (не повторять)

Эти вещи стоили времени и могут всплыть снова:

- **В apt отсутствовал пул `noble-updates`** — только `noble` и `noble-security`.
  apt стоял в неразрешимом конфликте (`bzip2` vs `libbz2-1.0`) и **не мог поставить
  ничего вообще**. Чинится файлом `/etc/apt/sources.list.d/shibuya-updates.sources`.
- **mosh и локаль.** `update-locale LC_ALL=` пишет *пустой* `LC_ALL`, на котором
  mosh-server отказывается стартовать. Плюс macOS шлёт несуществующий
  `LC_CTYPE=UTF-8`; `AcceptEnv` — аддитивная директива, drop-in её не переопределяет,
  поэтому стоковую строку в `sshd_config` пришлось закомментировать.
- **mosh 1.4.0 падает (SIGABRT) при нулевом размере терминала.** Всплывает только
  при headless-тестировании через `script -q /dev/null` на macOS. С настоящим
  терминалом всё работает — не тратить время на «баг».
- **`cmd | grep -q` под `set -o pipefail` врёт при совпадении**: grep выходит первым,
  отправитель ловит SIGPIPE. В `lib/common.sh` для этого есть `contains()`.
- **`write_file` всегда возвращает 0**, факт изменения — через `changed()`.
  Раньше возврат 1 = «без изменений» ронял скрипты на повторном прогоне.
- Официальный apt-репозиторий helm (`baltocdn.com`) отдаёт неполную цепочку
  сертификатов — helm ставится бинарём с `get.helm.sh`.

## Безопасность

На машине креды от трёх GKE-кластеров (два прода) и GitHub-ключи, и она будет
торчать в интернет через проброс.

- Ключи Pi (`id_lvn`, `id_gh`) **отдельные от ключей Мака** — отзываются независимо.
- Ключ клиентского контекста `pt` намеренно **не положен**. Если понадобится:
  `sudo SHIBUYA_WITH_PT=1 ~/shibuya/bootstrap.sh --only 70`
- `gcloud auth application-default login` **не делали** — ADC-токен самая ценная
  вещь на машине, обычного `gcloud auth login` хватает.
- Процедура на случай компрометации — в `RUNBOOK.md`, раздел «Если машину
  скомпрометировали». Написана заранее осознанно.

## Где что лежит

| Путь | Что |
|---|---|
| `~/shibuya/` (Мак) | git-репозиторий со скриптами, `RUNBOOK.md`, этот файл |
| `~/shibuya/` (Pi) | та же копия, синхронизируется `deploy.sh` |
| `/etc/shibuya/move.env` | WiFi родителей + токен Telegram, root:600, **не в git** |
| `/etc/shibuya/backups/` | оригиналы всех тронутых системных файлов |
| `/etc/shibuya/shell-tmux.sh` | авто-attach и перехват `exit`, подключается из обоих rc |
| `~/.ssh/config` (Мак) | блок `shibuya:hosts` с алиасами |
